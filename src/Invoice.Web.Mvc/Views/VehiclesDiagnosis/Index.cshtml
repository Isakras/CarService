@using Abp.Application.Services.Dto;
@using Invoice.VehiclesDiagnosis.Dto;
@using Invoice.Web.Startup;
@using Invoice.Web.Models.Common;
@using X.PagedList;
@using X.PagedList.Mvc.Core;
@using X.PagedList.Web.Common;
@model X.PagedList.IPagedList<Invoice.VehiclesDiagnosis.Dto.VehicleDiagnosisDto>  //PagedResultDto<VehicleDiagnosisDto>
@{
    ViewBag.Title = L("VehicleDiagnise");
   ViewBag.CurrentPageName = PageNames.VehiclesDiagnosis;
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <h1>@L("VehicleDiagnise")</h1>
            </div>
         @*    <div class="col-sm-6">
                <a href="javascript:;" data-toggle="modal" data-target="#VehicleCreateModal" class="btn bg-blue float-sm-right">
                    <i class="fa fa-plus-square"></i>
                    @L("Create")
                </a>
            </div> *@
        </div>
    </div>
</section>
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-start mb-3">
                            <div class="col-12 col-md-3 mb-2">
                                @*  <div class="col-md-5"> *@
                                    <label>&nbsp;</label>
                                    <br />
                                    <a class="btn btn-primary  text-white border-0 text-uppercase  " href="@Url.Action("Create")">
                                        + Diagnoze e Re
                                    </a>
                            </div>
                            <div class="col-12 col-md-9">
                                @await Html.PartialAsync("~/Views/VehiclesDiagnosis/Index.AdvancedSearch.cshtml")
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                                <table id="VehicleTable" class=" table table-striped table-bordered table-sm text-nowrap" style="table-layout:auto;">
                                <thead>
                                    <tr>
                                        <th>@L("ClientName")</th>
                                        <th>@L("DiagnosisDate")</th>
                                        <th>@L("ProblemDescription")</th>
                                        <th>@L("FixDescription")</th>
                                        <th>@L("Cost") </th>
                                        <th>@L("Comments")</th>
                                        <th>@L("Vin")</th>
                                        <th>@L("PlateNo")</th>
                                        <th>@L("Color")</th>
                                        <th>@L("WorkerName")</th>
                                        <th style="width: 150px">@L("Actions")</th>
                                    </tr>
                                </thead>
                                <tbody> @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.ClientName</td>
                                            <td>@item.DiagnosisDate.ToString("yyyy-MM-dd")</td>
                                            <td>@item.ProblemDescription</td>
                                            <td>@item.FixDescription</td>
                                            <td>@item.Cost</td>
                                            <td>@item.Comments</td>
                                            <td>@item.Vehicle.VIN</td>
                                            <td>@item.Vehicle.PlateNo</td>
                                            <td>@item.Vehicle.Color</td>
                                            <td>@item.Mechanic.FullName</td>

                                        </tr>
                                    }
                                </tbody>
                            </table>
                            @Html.PagedListPager(Model, page => Url.Action("Index", new
                                {
                                    page,
                                    keyword = ViewBag.Keyword,
                                    fromDate = ViewBag.FromDate?.ToString("yyyy-MM-dd"),
                                    toDate = ViewBag.ToDate?.ToString("yyyy-MM-dd"),
                                    VehicleId = ViewBag.VehicleId,
                                    municipality = ViewBag.Municipality,
                                    region = ViewBag.Region,
                                    agentId = ViewBag.AgentId,
                                    keywordType = (int?)ViewBag.KeywordType,
                                    sortBy = ViewBag.SortBy,
                                    sortOrder = ViewBag.SortOrder
                                }), new X.PagedList.Mvc.Core.PagedListRenderOptions
                           {
                               DisplayItemSliceAndTotal = false,
                               ContainerDivClasses = new[] { "pagination-container" },
                               LiElementClasses = new[] { "page-item" },
                               PageClasses = new[] { "page-link" },
                               DisplayLinkToFirstPage = X.PagedList.Mvc.Core.PagedListDisplayMode.Always,
                               DisplayLinkToLastPage = X.PagedList.Mvc.Core.PagedListDisplayMode.Always
                           })

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>